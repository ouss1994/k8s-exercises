apiVersion: v1
kind: Service
metadata:
  name: {{ include "webapp-color.fullname" . }}
  labels:
    {{- include "webapp-color.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webapp-color.selectorLabels" . | nindent 4 }}
